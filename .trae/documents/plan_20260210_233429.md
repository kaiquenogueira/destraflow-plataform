Baseado na sua solicitação e na análise de como as campanhas funcionam (elas tiram uma "foto" dos leads no momento da criação), o Kanban será uma ferramenta poderosa de **Segmentação Pré-Campanha**.

### 1. Implementação do Kanban (Pipeline de Vendas)

O Kanban servirá para organizar visualmente seus leads antes de disparar campanhas. Ao mover um card, você estará alterando a `Tag` do lead, preparando-o para futuras campanhas segmentadas.

**Estrutura Proposta:**
*   **Colunas Automáticas:** Baseadas nas tags do sistema (`COLD`, `WARM`, `HOT`, `CUSTOMER`, `LOST`).
*   **Cards:** Exibirão Nome, Telefone e um botão rápido para "Iniciar Conversa" (WhatsApp Link).
*   **Ação de Arrastar:** Ao soltar um lead em uma nova coluna, o sistema atualizará a tag dele no banco de dados instantaneamente.
*   **Tecnologia:** Utilizarei `@dnd-kit`, que é a biblioteca mais moderna e performática para React atualmente.

**Arquivos:**
*   `src/actions/leads.ts`: Adicionar ação `updateLeadTag` para persistir a mudança.
*   `src/components/leads/kanban-board.tsx`: O quadro interativo.
*   `src/app/(dashboard)/leads/page.tsx`: Adicionar uma aba/toggle para alternar entre "Lista" e "Kanban".

### 2. CI/CD (GitHub Actions)

Para garantir que as alterações no Kanban ou no Webhook não quebrem o sistema, configurarei uma pipeline de integração contínua.

**Fluxo da Pipeline:**
1.  **Trigger:** A cada `push` ou `pull_request` na branch `main`.
2.  **Lint:** Verifica erros de sintaxe e padrão de código.
3.  **Testes:** Roda os testes unitários (como o do Webhook que criamos) usando `vitest`.

**Arquivo:** `.github/workflows/ci.yml`

***

### 🚀 Plano de Execução

1.  **Kanban**:
    *   Instalar dependências (`@dnd-kit/core`, `@dnd-kit/sortable`).
    *   Criar a Server Action para atualizar tags.
    *   Construir os componentes visuais do Kanban.
    *   Integrar na página de Leads.

2.  **CI/CD**:
    *   Criar o arquivo de workflow do GitHub Actions.

Podemos prosseguir com essa implementação?
